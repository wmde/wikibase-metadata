name: wikibase-metadata

services:
  database:
    image: postgres:16-alpine
    volumes:
     - backend-database:/var/lib/postgresql/data:z
    expose:
      - 5432
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_INITDB_ARGS: --auth-host=scram-sha-256 --auth-local=scram-sha-256
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d app"]
      interval: 5s
      timeout: 3s
      retries: 12

  backend:
    image: wikibase-metadata-backend
    build: 
      context: ./backend
    dns:
      1.1.1.1
    group_add:
      - ${TEAM_GID}
    expose:
      - 8000
    volumes:
      - ./logs/:/app/logs/:z
      - ./data/wikibase-data.latest.db:/app/data/wikibase-data.latest.db:z
    environment:
      AUTH_TOKEN: ${AUTH_TOKEN}
      DATABASE_URL: postgresql+asyncpg://app:app@database:5432/app
      LOG_LEVEL: ${LOG_LEVEL}
      SETTINGS_FILE: ${SETTINGS_FILE}
    restart: unless-stopped
    depends_on:
      database:
        condition: service_healthy

  frontend:
    image: wikibase-metadata-frontend
    build:
      context: ./frontend
      args:
        VITE_BACKEND_URL: http://backend:8000/graphql
    expose:
      - 3000
    environment:
      VITE_BACKEND_URL: http://backend:8000/graphql
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - 80:80
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on: [backend, frontend]
    restart: unless-stopped

volumes:
  backend-database:
