name: wikibase-metadata

services:
  backend:
    image: wikibase-metadata
    build:
      dockerfile: ./Dockerfile
    ports:
      - 8000:8000
    volumes:
      - ./db/:/app/data/:z
      - ./logs/:/app/logs/:z

  frontend:
    image: wikibase-metadata-frontend
    build:
      context: frontend
      dockerfile: ./Dockerfile
    ports:
      - 80:3000
