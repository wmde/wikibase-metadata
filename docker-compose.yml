# Docker compose setup for developing the app
# Use with:
#   UID=$(id -u) GID=$(id -g) docker compose up --build

services:
  app:
    build: .
    volumes:
      - type: bind
        source: .
        target: /workspace
    ports:
      - 8000:8000
    user: "${UID}:${GID}"
